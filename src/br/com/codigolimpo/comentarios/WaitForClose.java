package br.com.codigolimpo.comentarios;

public class WaitForClose {
    /*
    Comentários como este não ajudam em nada, é mais fácil olhar diretamente no código.

    // Métodos ultilizados para retornar quando "this.closed" for "true". Ativa uma exeção se o "timeout" for alcançado.
    public synchronized void waitForClose(final long timeoutMillis) throws Exception {
        if(!closed) {
            wait(timeoutMillis);
            if (!closed)
                throw new Exception("MockResponseSender could not be closed");
        }
    }
     */
}
